# Windows-only project: keep CRLF in the working tree so scripts and configs
# open correctly in every Windows editor, including Notepad.
* text=auto eol=crlf

# Never normalise these.
*.png binary
*.jpg binary
*.ico binary
